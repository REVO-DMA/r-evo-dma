/**
 * Handles all of the window controls and events via IPC.
 * @param browserWindow The BrowserWindow to control and attach events to.
 */
export function titlebarProcessBridge(browserWindow: import("electron").BrowserWindow): void;
